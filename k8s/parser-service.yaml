apiVersion: v1
kind: Service
metadata:
  name: parser-service
  labels:
    app: parser
spec:
  type: ClusterIP
  ports:
  - port: 50051
    targetPort: 50051
    protocol: TCP
    name: grpc
  selector:
    app: parser
